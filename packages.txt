# Runtime the Kivy wheel dlopens at execution (the wheel bundles neither SDL2 nor GL).
# (sdl2-compat is what Arch ships now — it provides/replaces sdl2; matches the bench Pi.)
sdl2-compat
sdl2_image
sdl2_ttf
sdl2_mixer
mesa
libglvnd
# Touch: libmtdev lets Kivy's default probesysfs input provider auto-detect the USB touch panel
# (QDtech MPI7003 1024x600 on the bench Pi 3B). Without it touch is dead; with it, no Kivy config
# change is needed.
mtdev
git
sudo
openssh
networkmanager
wpa_supplicant
wireless-regdb
mpg123
alsa-lib
ttf-dejavu
# Build deps so pyenv can compile CPython 3.13 (see build.sh). We pin the app's venv to 3.13 —
# which HAS aarch64 wheels for Kivy et al. — instead of Arch's rolling python (currently 3.14,
# ahead of Kivy's wheels). App deps then install as wheels (no rust/gcc needed for them), and
# field `pip install` updates stay wheel-based. Toolchain kept in the image (builds CPython; cheap
# insurance for the rare source build).
base-devel
openssl
zlib
xz
bzip2
readline
sqlite
libffi
ncurses
